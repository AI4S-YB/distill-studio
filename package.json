{
  "name": "distill-studio",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "github:sync-secrets": "node scripts/sync-github-secrets.mjs",
    "release:preflight": "node scripts/release-preflight.mjs",
    "updater:prepare-config": "node scripts/write-tauri-release-config.mjs",
    "tauri:build:release": "npm run updater:prepare-config && tauri build --config src-tauri/tauri.release.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
